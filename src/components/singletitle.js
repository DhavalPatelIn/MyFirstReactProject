import React from "react";

function SingleTitle({SingleTitle}) {
    return(
        <div className="title--wrap text-center">
            <h2>{SingleTitle}</h2>
        </div>
    );
}

export default SingleTitle;